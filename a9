public class Recursion
{
    public int Fib(int n)
    {
        if (n == 0)
            return 0;
        else if (n == 1)
            return 1;
        else
            return Fib(n-1) + Fib(n-2);
    }
    
    public int Mult(int x, int y)
    {
       if (y == 1)
           return x;

    }
}
