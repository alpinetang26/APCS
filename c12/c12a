public class FunLoops
{
   
    static boolean Sum(int y)  //y is number being added to
   {
       int sum = 0; 
       int increase = 1;
       while (sum < y)
       {
           sum += increase;
          
        }
       if (sum == y)
            return true;
       else   
        return false;
    }
}
